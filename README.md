# How to use my config
## Install stow
### Macos
`brew install stow`
### Arch Linux
`sudo pacman -S stow`
## Install using stow
`git clone https://github.com/blissed-out/dotfiles` 
`cd dotfiles` 
`stow <folder-name>` 

